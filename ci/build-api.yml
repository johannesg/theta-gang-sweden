version: 0.2
phases:
  install:
    commands:
      - cd app/lambda
      - npm ci
  build:
    commands:
      - npm run build
artifacts:
  base-directory: app/lambda
  files: 
    - index.js
    - index.js.map
    - schema.graphql